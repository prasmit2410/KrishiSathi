/**
 * config.js — Set your Render backend URL here for Netlify deployment.
 *
 * LOCAL DEVELOPMENT: Leave API_BASE as "" (empty string) — Flask serves both
 *   frontend and backend on the same localhost:5000 origin.
 *
 * PRODUCTION (Netlify + Render):
 *   Change API_BASE to your Render service URL, e.g.:
 *   const API_BASE = "https://krishi-sathi-api.onrender.com";
 */
const API_BASE = "";
